<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.example.volleyball.mapper.MemberMapper">

  <select
    id="findAll"
    resultType="com.example.volleyball.entity.Member">
    SELECT * FROM members WHERE deleted = false
  </select>


  <insert id="insert">
    INSERT INTO members (name, uniform_number, join_date)
    VALUES (#{name}, #{uniformNumber}, #{joinDate})
  </insert>


  <update id="update">
    UPDATE members
    SET name = #{name},
    uniform_number = #{uniformNumber},
    join_date = #{joinDate}
    WHERE id = #{id}
  </update>

  <update id="delete">
    UPDATE members SET deleted = true WHERE id = #{id}
  </update>

</mapper>
